#include <iostream>
using namespace std;
/*typedef struct employee
{
    int eid;
    char favChar;
    float salary;
} ep;
union money
    {
        int rice;
        char car;
        float price;

    };
    */
int main()
{
    // structure sunions asnd enums foundations ion c++
    // structure is a user defined datatype for heterogeneous elements
    /*  ep E;

    E.eid=42;
    E.favChar='a';
    E.salary=3234;
    cout<<"The employees information are :"<<"salary="<<E.salary<<endl<<"favourite character="<<E.favChar<<endl<<"Employee id="<<E.eid<<endl;
 */
    // unions i c=++
    // unions are the user defined datatypes in  c++ that uses a shared memory locations i.e the asllocated memory would be shared all along eith each other and only one can be accessed at a time//
  /*  money m1;
    m1.car='c';
    m1.price=12341254;
    m1.rice=21;
    cout<<"The informations obtained under the userdefined data type union are"<<"car="<<m1.car<<endl<<"price="<<m1.price<<endl<<"rice in kg="<<m1.rice<<endl;
   */


    return 0;
}
